const form = document.querySelector('form');
//Form is sybmitted in post and get type go to server or url..
//we have to access it beffore going to server

form.addEventListener( 'submit' , (e) => {
    e.preventDefault()
    //we need values

    

} );